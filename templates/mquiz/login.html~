{% extends "base.html" %}

{% block content %}


<h2>Login</h2>

{% if form.errors %}
<p>Your username and password didn't match. Please try again.</p>
{% endif %}


<form method="post" action="{% url django.contrib.auth.views.login %}">
{% csrf_token %}
	<div class="formblock">
		<div class="formlabel">{{ form.username.label_tag }}</div>
		<div class="formfield">{{ form.username }}</input></div>
	</div>
	<div class="formblock">
		<div class="formlabel">{{ form.password.label_tag }}</div>
		<div class="formfield">{{ form.password }}</div>
	</div>
	<div class="formblock">
		<div class="formlabel">&nbsp;</div>
		<div class="formfield">
            <input type="submit" value="Login" />
			<input type="hidden" name="next" value="{% url mquiz-home %}" />
			<a href="reset.php">Forgotten password?</a>	
		</div>
	</div>
	
</form>

<input type="hidden" name="next" value="{% url mquiz-home %}" />
</form>

{% endblock %}
